[project]
name = "nyc-taxi-warehouse"
version = "0.1.1"
description = "NYC TLC trip data modeled with dbt on Clickhouse and Snowflake at scale"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "clickhouse-connect>=1.6.0",
    "httpx>=0.28.0",
    "pyarrow>=25.0.0",
    "tqdm>=4.67.0",
]
